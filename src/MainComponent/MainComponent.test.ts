import MainComponent from './index';

describe('Main Component', () => {
  it('Should successfully test', () => {
    expect(MainComponent('Main Component')).toEqual('Test Main Component');
  });
});
